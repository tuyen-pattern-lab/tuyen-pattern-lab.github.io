<div class="{{styleModifier}}">
  <label for="{{id}}" class="form-label">{{label}}</label>
  <textarea class="form-control" id="{{id}}" rows="{{rows}}" placeholder="{{placeholder}}" {{#if disabled }}disabled{{/ if }} {{#if readOnly }}readonly{{/ if }}>{{value}}</textarea>
</div>